int main() {
    // We can initialize a scalar with a single value:
    int x = 3;
    double y = 3.5;
    int *px = &x;
    // In C++, this is a form of copy-initialization (C doesn't name the forms of initializaion like
    // C++ does)
}
