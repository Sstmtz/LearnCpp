// std::atomic_ref: Atomic reference
// Basically the same as std::atomic but works with reference,
// while std::atomic always copies the value it it provided.

#include <atomic>

int main() {
    return 0;
}
